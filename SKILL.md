---
name: qversion-social-short-video
description: Create production-ready short-video prompts for JIN's four Q-version characters—菲比、糯糯、Q版 Sera 与咕嘎—from rough ideas, references, feedback, or prior generations. Default to 15 seconds unless the user specifies another duration, preserving character identity, physical blocking, motivated POV, prop continuity, staggered audio, and task-specific failure guards.
---

# Q版社交短视频导演 Skill

这是 JIN 四小只 Q版短视频的专属导演 Skill，负责把零散想法、场景、动作或成片反馈理解成可直接生成的提示词。优先服务 9:16、约 8–15 秒、动作与情绪驱动的真实空间 POV 社交媒体内容。

本 Skill 必须能够独立完成 Q版短视频的需求澄清、第一帧与空间设计、动作编排、参考资产验收、最终 Prompt 和成片复盘；不以另一个视频提示词 Skill 可用为前提。

## 粗剧情入口与交付前复核

1. 先按“东坡肉”范围法读用户的粗略想法，分开记录明确要的、明确排除的和未定的内容。“不要 A、不要 B”只排除 A、B，不能据此推断“只要当前提到的几位角色”；顺带提及角色也不等于确认了完整出镜名单。
2. 将本次仍需确认的三项合并成一轮短问题：**角色**是否只有已提及者（Sera 是 Q 版还是成女）、**场景**地点或参考图是否已定／是否需要提出方案、**道具资产**是否有额外要加入或锁定的专属物件及参考图。已明确的信息只复述，不重复追问；普通场景或道具不自动要求制作独立资产。
3. 得到确认后，继续收束本片实际需要的角色、参考图职责、第一帧机位、动作过程与结束状态，再按下文读取固定角色资料、适用模板和其他必要规则，形成内部 Prompt 初稿。用户要求直接写且缺口允许保守补足时，仍按下文“粗略描述的提问边界”执行；未答复不算已确认。
4. 对外输出前，用本轮确认的信息和实际参考资产复核内部初稿：角色身份与数量、Q 版发声权限、场景方向、道具实体与状态、POV 动机、动作因果和结束状态。发现冲突先修初稿，再交付可复制的最终 Prompt；不要等初稿写好后才首次读取本 Skill 的角色规则。

## 全新创作默认 15 秒；继承原稿时沿用 BASE 时长

当用户要求全新 Q版社交短视频，且没有指定时长或已有 BASE 时，默认生成 **15 秒**。

- 全新题材且用户未指定时长时使用 15 秒。用户明确指定其他时长，或明确要求复刻／最小修改一份已有时长的 BASE 时，沿用其时长；用户要求把 10 秒稿扩成 15 秒时才改变该 BASE 的时长；
- 不得因为剧情简单、只有一个笑点、成本、节奏判断或模型偏好，自行缩短或延长；不要用“默认 15 秒”覆盖已选 BASE 的 10 秒时长；
- 用户只提供题材、画面或动作而未提及时长时，仍按 15 秒编排；
- 若目标平台的硬性限制无法生成 15 秒，应先向用户说明限制并确认替代时长，不得静默改成其他秒数；
- 时长确定后，制作策划中各段预估时长之和必须覆盖并核对该目标时长；最终生成 Prompt 是否保留精确时间戳，按下文“双层时长”决定。


## 证据基线：不得覆盖 Claude 原经验

开始工作前，读取 `references/claude-original-methodology.md`。

该文件保存长期协作中反复试错、验证、迭代得到的原始方法论。它是本 Skill 的经验基线：

- 不删除其中任何经验；
- 不把尚未验证的问题偷偷改成确定规则；
- 新经验只能以增量方式补充；
- 若新方法与原经验冲突，先记录冲突与样本，不直接覆盖原文；
- 用户提供新的生成结果时，保留“提示词 + 成片反馈”作为证据，再决定是否升级规则。

下面的“当前生产经验”来自用户已经实际生成、并明确反馈为喜欢或爆款的样本。它们属于当前系列的高价值增量证据，但不意味着所有题材都必须套同一个结构。

分析实际成片、定位失败或吸收新经验时，同时读取 `references/validated-production-notes.md`。该文件保存“原 Prompt／实际参考资产／成片现象／可复用结论”，用于追溯证据，不把案例原句机械塞进最终 Prompt。

## 四小只固定角色库与出镜选择（最高优先级）

开始工作时读取：

`references/four-character-canon.md`

该文件保存菲比、糯糯、Q版 Sera、咕嘎的固定身份、性格行为、拟声词和物理表演边界。新聊天不得依赖记忆猜测角色资料。

出镜规则：

- 用户说“四小只”“四个一起”或明确列出四人时，四人都是主线活动角色，都要对同一个中心事件作出不同贡献；
- 用户明确指定其中一人、两人或三人为本片出镜名单时，只使用这些角色，不为了凑齐四人自动加入其他角色；粗想法中只是顺带提及几位时，先按上方三项确认核对名单是否完整；
- 当前请求没有点名的角色，不得因为旧模板、成功案例、上一版 Prompt 或参考图中曾经出现而自动继承进主线；只有用户明确要求该角色作为后景 OB / IP 活水印时，才允许保留为非主线角色；
- 用户明确要求某人作为后景 OB、彩蛋或 IP 活水印时，才启用背景角色模式；
- Q版 Sera 是四小只的正式成员，不再默认降为后景 OB；
- 若用户明确要求 Sera 使用后景 OB 模式，再读取 `references/sera-background-ob-watermark.md`；
- 用户给出的角色—参考图映射永远高于默认图序，不得交换身份；
- POV 的手或手臂不计作额外角色。

如果“她们”“三小只”等指代在当前聊天中无法唯一确定，且不同选择会改变成片，先用一个简短问题确认；能够从当前输入、参考图或已点名角色唯一判断时直接执行。

## 欢迎回家版本与新聊天入口

先判断用户本轮要做什么，再打开对应文件。将用户贴回的聊天记录视为本轮优先输入；文件中对样本状态的称呼不能覆盖聊天记录。

| 用户说法 | 读取 | 怎样使用 |
|---|---|---|
| “原版”“爆款版”“最喜欢的欢迎回家”“照原版改” | `references/welcome-home-10s-user-original.md` | 用户贴回的 **10 秒原稿**；按 Minimal-Delta Mode 保留时长、动作顺序与成功措辞，只改指定部分。用户称成片爆款，但没有从本聊天验明每句提示词的单独作用。 |
| “15 秒加强版”“把 10 秒扩成 15 秒” | 先读 10 秒原稿，再读 `references/welcome-home-15s-template.md` | **15 秒是本聊天中助手写的加强候选稿**；先补动作过程与 POV 回应，不将它称为已生成、已验证爆款。 |
| “门外首帧修正版” | `references/welcome-home-15s-template-door-outside-variant.md` | 独立的门内外空间修订候选；只有用户选择这一版才以它为 BASE。 |
| “欢迎回家的感觉，但换剧情” | `references/welcome-home-care-pattern.md` | 借“已等待→分别照顾→POV 回应→温暖收尾”的功能链；拖鞋、滑铲、钥匙不是每条的必备动作。 |

**先核对四件事：**本轮角色名单、BASE／时长、第一帧的门内外位置、最后一帧。聊天已明确时直接写；仅当缺口会改变这些结果时才简短提问。若用户只说“写个欢迎回家”，且当前聊天同时给出 10 秒原稿和扩写 15 秒的选择，沿用最近明确选定的时长；无法判定时先确认版本，不把 15 秒默认值冒充原稿。

复用已验证成像固定层时，保留：`真实手机实拍风格，第一人称 POV 手持手机拍摄，镜头保持稳定，只带轻微自然的人体晃动。` 风格和拍摄载体是两个独立条件。第一帧先写机位于门外、门关闭，再写门打开后显露的室内暖光；不要让“傍晚”先于空间关系决定画面。历史原文的潜在空间歧义保持为证据，实际新稿要写清门内外。

历史 10 秒稿的 Sera 发声写作“selasela”。**JIN 在 2026-09-25 明确确认今后 Q 版 Sera 固定拟声词为 `Sela`**。保存原稿引用时不改历史字句；创作新稿或改稿时统一写 `Sela`，除非用户本轮改变设定。作者层的 `~`、`！`用于理解语气；成品只写实际发声和可听见的语气，不解释符号规则。

保留 10 秒原稿与 15 秒草稿各自的证据状态。复盘成片前不得把“助手写出”“用户喜欢文字结构”和“实际生成成功”合并成同一种验证。

## 已验证 Prompt 继承模式（Minimal-Delta Mode）

当用户说“按原版改一下”“保持结构只换动作”“其他不变”“基于成功版做一个变体”等继承式要求时，必须先确定 BASE，再进行最小差异修改。

### 1. 先找 BASE

BASE 的优先顺序：

1. 用户在当前请求中明确指定的 Prompt、文件或版本；
2. 用户明确称为“原版”“成功版”“爆款版”的 Prompt；在《欢迎回家》这组聊天里默认指 10 秒用户贴回原稿，不指后来撰写的 15 秒候选稿；
3. 当前任务最近一次被用户明确接受的版本；若用户最近明确选定 15 秒加强版，先沿用该目标时长，再核对其稿件仍处于候选状态。

若存在两个内容不同、都有可能被称为“原版”的候选，并且选择会改变成片，先简短确认。不得因为某个 variant 更新、更长或看起来更合理，就静默把它当成母版。

### 2. 先分类，再修改

在动笔前把 BASE 内容分成三类：

- **PRESERVE**：用户未要求改变的内容。包括原时间结构、事件顺序、镜头因果、场景、角色行为、道具连续性、音频位置、结尾构图、成功措辞和失败保险。
- **CHANGE**：用户直接点名要替换、删除或新增的内容。
- **DEPENDENCY CHANGE**：为了让 CHANGE 在物理、时序或连续性上成立而不得不联动的最少内容，例如动作落点改变后需要调整站位，换掉道具后需要同步交接对象。

只允许修改 CHANGE 与确实必要的 DEPENDENCY CHANGE。不得借“优化”为由重排未点名段落、替换成功措辞、改写其他角色动作、改变结尾或重做整条 Prompt。

### 3. 角色名单继承防错

角色名单不属于可以盲目继承的 PRESERVE 项：

- 以用户这一次明确点名的角色为准；
- 用户这次没有点名的角色，不得因为 BASE、旧模板、参考图或上一版本里出现过而自动进入主线；
- 只有用户明确要求后景 OB / IP 活水印时，未进入主线的角色才可以作为背景存在；
- 删除旧角色后，同步删除其动作、拟声词、参考图职责和失败保险，这是必要的 DEPENDENCY CHANGE；
- 不得用模板原有人数覆盖用户当前角色名单。

### 4. 最小差异检查

交付前逐项核对：

1. BASE 是否选对；
2. 用户点名的变化是否全部完成；
3. 每一处额外变化是否能解释为必要的 DEPENDENCY CHANGE；
4. PRESERVE 内容是否保持原顺序、原职责与原成功逻辑；
5. 是否误继承了本次未点名角色。

如果无法说明某处为什么必须改变，就恢复 BASE 原文。

### 模板用于学习密度，不用于制造冗长

完整模板提供的是**信息组织与控制密度基准**，不是要求每条 15 秒提示词都复制相同字数、段落数量或禁止项数量。

生成最终提示词时：

- 已经在时间轴里清楚表达过的事实，不为了“保险”在角色要求、动作要求和反向约束里重复三四次；
- 反向约束只保护当前视频真实容易崩的剧情事实、角色身份、道具连续性、一次性动作、时序和 POV 因果；
- 不要写与当前题材无关的禁止项。例如当前视频根本没有某个角色，就不要额外写“不要出现该角色”，除非已有实际生成证据表明模型会错误召回它；
- 不要在最终视频提示词里写“和欢迎回家不同”“不要像某旧案例”之类对模板或旧案例的比较说明；直接告诉视频模型当前画面要发生什么；
- 如果一个约束已经能由更具体的正向动作描述解决，优先保留正向描述，不再堆重复否定词；
- 保持足够细节来控制关键动作，但字数应服务生成稳定性，不把“写得更长”当作“控制得更好”。

最终目标是：**保留《欢迎回家》那种动作过程、角色差异、音频时序和失败保险的清楚程度，同时删除当前题材不需要的重复信息。**

## 核心导演原则

### 从粗略想法到可执行第一帧（硬规则）

> 视频模型更服从具体、可视化的空间与动作语句，而不是上层的抽象意图。

这条规则决定写作优先级：用户说的“路过”“偶遇”“在旁边”“跟着”“走向夕阳”“乡村街边”等词，先视为创作意图；最终 Prompt 必须把它们翻译成视频模型能直接画出来的第一帧、空间关系、身体状态和动作过程。抽象词不能与具体时间线互相矛盾。

排动作时间轴前，先锁定：

1. **第一可见帧**：POV 已经在谁的旁边，还是远处才看见；角色在画面什么区域、相距多远，手里已经拿着什么；
2. **摄影机路径**：POV 是向前经过、迎面靠近、停下回看，还是保持原位；不得用“继续向前”代替未确定的路过方向；
3. **场景轴线**：摄影机朝向哪里，路径如何延伸；会影响画面的房屋、树木、门、栏杆等分别在哪一侧；
4. **角色初始状态**：角色已经围在一起、已经吃东西、已经举起相机，还是动作要在镜头内开始；
5. **建立过程是否占时**：从远处走近、镜头缩放、转头寻找、空镜建立都是真实生成动作，会消耗目标时长，不能当作免费的铺垫。

**故事开始必须等于第一可见帧。** 如果用户想要“POV 已经从三小只旁边路过，然后立即互动”，第一帧就应处于可互动距离；不得先写“远处出现三个小身影 → POV 继续向前 → 随距离接近进入中景”。后者实际定义的是“走向她们”，即使同段仍写“路过”，视频模型也更可能执行那组具体、可视化的靠近指令。

交付前做一次**抽象意图—具体执行一致性检查**：

- 上层意图说的关系，第一帧是否已经看得见；
- 每个具体空间句和运镜句，是否在帮助该意图，而不是偷偷改写它；
- 是否存在一条未被用户要求、却会消耗数秒的接近、缩放、寻找或转身过程；
- 若删掉“路过／偶遇／温馨”等抽象词，仅看具体动作，生成出来的故事是否仍是用户要的故事。

### 粗略描述的提问边界

用户不必一次把镜头语言说完整。若缺少的信息会明显改变第一帧、摄影机路径、环境左右轴、角色初始组合、关键道具状态或结尾，先把相关缺口合并成**一个短问题**确认，不要各问一轮，也不要静默发明远距离开场。

如果用户明确要求直接写、不等待确认，并且可以采用不改变核心事件的保守默认值，则默认：

- 第一帧直接进入核心事件成立的距离；
- 不凭空增加远景发现、长距离走近或无动机缩放；
- 普通环境只自然补全，不替用户写死会改变构图方向的地理；
- 将必要假设保持最少，且不改角色、事件顺序与结尾。

## 轻量资产默认值（硬规则）

普通 8–15 秒 Q版日常视频若已有本条真正出镜角色的身份资产，默认**直接写视频提示词**，不再制作组合场景图、动作姿态图、道具图或故事板。

以下内容属于可变层，由提示词生成：

- 普通卧室、客厅、玄关、门、床等生活空间，只写动作成立所需的必要锚点；
- 枕头、杯子、拖鞋、食物等没有独特世界观设计的日常道具；
- 敲门、开门、指向、转头、点头、走路、跑动、拥抱、拿取、放下等普通动作；
- 水汪汪的眼睛、开心、期待、犹豫等表情与微表演；
- POV 转向、摄影机上下晃动、轻微手持感和简单焦点变化。

不要把动作时间线中的每个阶段变成一张静态资产。不要因为多人同框、存在物理接触或摄影机需要转向，就自动要求额外资产或故事板。

仅在以下情况之一明确成立时，才评估额外资产：

- 用户主动要求制作该资产；
- 独特角色身份、世界观场景、专属道具或 VFX 的外观无法仅靠文字稳定定义；
- 镜头包含极端大姿势、高难度身体力学或复杂多人 blocking，且关键结果无法从现有资产推断；
- 需要精确跨镜继承构图／空间／姿态；
- 已有实际生成失败证明某个具体视觉事实反复漂移。

提出额外资产时必须说明“它只锁什么”以及“没有它具体会失败在哪里”。没有这个答案，就保持轻量流程。

把提示词当作**分镜脚本 + 导演笔记**，而不是静态画面说明。

优先描述角色和画面**如何从一个状态过渡到下一个状态**。每个关键动作都尽量包含：

1. **发力起点**：动作开始前的身体信号；
2. **过渡状态**：动作真正发生时的中间过程；
3. **结果状态**：动作完成后的衰减、停顿或稳定姿态。

不要用“跑过去、抱住、撞倒、开心、震惊”这种结果词单独承担动作控制。

短视频不是把“大故事”压缩到 10–15 秒，而是让一个小事件拥有完整、可读、可记住的状态弧线。

## 固定层与可变层

### 固定层：同一角色系列尽量保持稳定

固定层可直接沿用同一角色已验证的版本，包括：

- 拍摄 / 成像风格；
- 镜头类型；
- 角色比例、描边、上色、发型等辨识特征；
- 角色专属拟声词、说话方式或发声权限；
- 角色音色；
- 已验证且不需要随剧情变化的角色身份描述。

不要为了“优化文案”每天重新改写固定层。若已有版本稳定，优先复用。

### “实拍风格”与“POV 载体”必须分别继承

在《欢迎回家》母版及其变体中，`真实手机实拍风格`与`第一人称 POV 手持手机拍摄`是两个独立的 **PRESERVE** 字段：前者锁定成像媒介与画面质感，后者锁定摄影机是谁、怎样移动。不得把两者压缩成“真实第一人称”，也不得擅自改成“人的双眼 POV”。“双眼 POV”不是同义改写，它会同时改变摄影机载体与画面风格。

继承母版时优先逐字保留：

> 真实手机实拍风格，第一人称 POV 手持手机拍摄，镜头保持稳定，只带轻微自然的人体晃动。

只有用户明确要求取消手机拍摄、改为裸眼视角、监控、第三人称或其他摄影载体时，才允许改写这一固定层。猫眼、窗缝、镜面等特殊观察方式只修改当前动作与光学遮挡：默认仍由手持手机镜头靠近并对准观察口，不自动把手机 POV 改成人眼 POV。

### 当前社媒系列已验证的实拍融合固定方向

当用户继续制作目前这组“Q版角色进入真实生活空间”的社媒视频时，可优先复用以下视觉逻辑，而不是每天重新发明：

- 真实手机实拍质感；
- 第一人称 POV 手持手机；
- 镜头只保留轻微、自然的人体晃动；
- 真实居家、门廊、玄关等现实环境光；
- Q版角色不是贴纸或平面叠图，而是现实空间中的 2.5D 赛璐璐实体；
- 有圆润实体厚度、清晰粗黑描边；
- 头发为简化动漫块面，不转成毛茸茸写实毛发；
- 扁平上色、颜色干净、低阴影；
- 角色受真实场景光照影响，并有接地阴影、接触阴影、重量感与真实接触；
- 参考图为二次元 Q版 CG 时，不把角色真人化，不生成真人皮肤质感与真人五官。

具体头身比、体型、服装和特殊身体特征仍以当前角色参考和项目设定为准，不从一个角色样本泛化到全部角色。

### 当前四角色发声配置

菲比、糯糯、Q版 Sera、咕嘎默认都**不说正常完整人类语言**，只使用各自固定拟声词，除非用户明确改变 Q版设定：

- 菲比：`菲比啾比～～` / `菲比！`
- 糯糯：`糯糯～`
- Q版 Sera：`Sela～`
- 咕嘎：`咕嘎` / `咕咕嘎嘎！`

固定音色方向：

- 菲比：元气、活泼、兴奋；
- 糯糯：软糯、轻柔、可爱、反应慢半拍；
- Q版 Sera：温柔、安静、软萌；
- 咕嘎：活泼、调皮、有弹性。

不得交换四人的拟声词，不得让任何一人说正常完整句子。某个角色可以根据剧情保持安静，但安静必须是导演选择，不代表身份消失。

本 Skill 只处理 Q版 Sera。成女／非Q版 Sera 属于独立项目，不在本 Skill 内处理；不得把这里的 Q版拟声词限制带入成女项目。

### 作者层与视频提示词层必须分开

用户在构思阶段写 `~`、`！`、重复音节等，首先是给**写提示词的 AI**理解情绪强弱、拉长、撒娇、兴奋或短促感的作者层标记。

最终交给视频模型的提示词：

- 可以直接保留最终确定的拟声词文本；
- 可以直接写“兴奋地喊”“满足地拉长声音”“轻轻地发出”等实际表演方向；
- **不要再解释**“`~` 代表拉长音”“`！` 代表兴奋”这种元说明；
- 不把写提示词的方法论、符号解释或作者备注塞进最终生成提示词。

### 可变层：每条视频重新导演

每次重新设计：

- 地点与必要空间锚点；
- 时间、光线氛围；
- 本条视频真正会使用的关键道具及必要位置关系；
- 动作阶段；
- 情绪结构；
- 角色行为差异；
- 音频时序与空间距离；
- 结尾记忆点。

## 场景空间：只写动作成立所需的必要锚点

当前爆款 / 高满意样本显示，真实居家短视频不需要把整个空间变成建筑平面图。

只写**会影响动作、视线、移动或道具连续性**的空间事实。例如：

- 玄关欢迎：门、拖鞋、钥匙盘；
- 披萨外送：前门透明玻璃、门垫、石板小路、远处大门；
- 睡眠照顾：沙发、被单、电视及其作为光源 / 噪音源的存在。

不要无意义规定所有家具精确左 / 右 / 前 / 后位置。空间描述越多不等于越稳定；只有在某个位置关系会决定动作是否成立时才写死。

空间写作检查：

1. 角色从哪里进入 / 离开？
2. POV 为什么能看见目标？
3. 关键道具在哪里，角色怎样接触到？
4. 后续动作需要保留哪条移动路径？

除此之外允许模型自然补全生活空间。

## 15 秒默认节奏

当用户没有另行指定且目标约为 15 秒时，优先从 **5–7 个动作 / 状态阶段**构建，不等于强制 5–7 个剪辑镜头。

一种可用思考方式：

```text
建立状态 / 预期
→ 第一次行动
→ 发生阻力、异常或第二次行为
→ 升级 / 反转 / 情绪加深
→ 角色即时反应
→ 收尾动作
→ 稳定定格 / 记忆点
```

允许合并节点。若内容简单，不为了凑数量硬塞事件。

约 8–10 秒时，优先单一情绪弧线和 3–5 个动作阶段；不要机械加入完整反转。

30 秒以上优先拆为多个独立镜头，让每镜服务单一叙事目的。

### 策划秒数与模型时间控制分层

- **策划层**：先为动作／状态阶段估算约几秒，用来判断 15 秒是否装得下、生成要拆几段、剪辑怎样组合；这些秒数由策划者掌握。
- **提示词层**：精确时间戳是可选控制，不是策划表的强制抄写。简单动作、明确节拍或已验证模型需要时可以保留；复杂动作、自然表演或连续因果更重要时，改用“阶段1／阶段2”或“镜头1／镜头2”的顺序描述，让模型自然分配段内节奏。
- 若一条 Q版日常视频本来就是同地点、同一 POV 下的连续互动，可以作为一段生成；若它需要数个镜头或生成片段共同完成故事，先规划整场共享锚点、每镜职能与策划时长，再决定逐段生成与剪辑。复杂长篇应另行使用通用视频导演工作流。

策划表中的秒数仍需覆盖目标成片长度；删掉模型 Prompt 中的时间戳，不等于放弃时长策划。

### 从 10 秒加强到 15 秒时，不要机械增加剧情

当前《欢迎回家》10 秒成功样本与后来提出的 15 秒加强方向说明：一个 10 秒结构已经有效时，延长到 15 秒优先做的是：

- 补足搬、跑、滑、接、摸、放下等动作的中间状态；
- 增加角色之间的真实接触或 POV 回应；
- 给微表情、犹豫、确认、减速、放松留时间；
- 让每个角色的行为选择更完整。

不要为了“15 秒必须有反转”硬加与主题无关的新剧情。

## 动作写法

关键动作优先写成因果链。例如：

```text
小短腿蹬地加速
→ 身体前倾
→ 双臂张开
→ 接触瞬间摄影机受到轻微反作用后仰
→ 手臂环抱收紧
→ 身体张力逐渐释放并稳定下来
```

重点不是词多，而是：

- 中间状态不能消失；
- 接触动作要有物理反馈；
- 动作结束要有衰减，不要瞬间冻结；
- 同一时刻不要让角色、摄影机、台词和环境同时发生多个高强度事件。

### 多角色动作主次（试片中的自检）

多人同框且动作容易串台时，按**动作阶段**先定一个主要可见目标，再写主行动者或协作双方、作用对象与结束状态；用 `@角色名＋具体动作词` 写出发力／触发 → 过程 → 接触或反作用 → 稳定结果。其他角色继承固定身份、比例与起始站位，只在事件需要时给等待、目光、协作或因果反应；另一条无关的完整主动作放到后续阶段。摄影机跟随、环境声光反馈可以同时发生，但须服务当前目标。

这是依据 JIN 已验收的 LibTV 外部案例提出的**待本系列实测写法**，不代表已证实的视频模型内部机制或防止人物变形的通用结论；不强制每镜只能一人动，也不改变已经成功的《欢迎回家》母版。需要两人配合递接或对招时，保留双方动作并写清先后、接触与反作用。

### 从现实身体逻辑反推动作（硬规则）

写动作前先判断：角色的身体位于空间哪一侧、朝向哪里、脚或身体的支撑点在哪里、重心怎样移动、物体或环境给了什么反作用力、动作最后怎样稳定。把答案写进动作过程，而不是只写情绪标签或结果词。

例如“晕船”不能只写“趴在船边很难受”；应先保证角色身体和双脚仍在船内，再确定背对或侧对镜头、上半身如何倚靠船沿、双手如何支撑、身体怎样随船摇摆以及重心始终没有越过船沿。

每个关键动作都优先具备：

`身体位置与朝向 → 发力或失衡信号 → 连续运动过程 → 接触／反作用 → 减速与稳定结果`

这不是要求堆砌解剖术语，而是让视频模型知道这个动作在现实空间中如何成立。

### 小体型 Q版角色要写重量与尺寸造成的动作结果

当前系列中的“巴掌大小 / 小团子”角色，魅力经常来自尺寸与物理反差。若道具相对角色很大，明确表现：

- 抱起后身体微微后仰；
- 小跑时左右摇晃；
- 踮脚才能放下；
- 放稳后才松手；
- 接过钥匙等物件时身体轻轻下沉。

不要只写“拿着大拖鞋”“拿着钥匙”，要让物体的大小和重量进入角色动作。

## 多角色：用行为选择区分性格

多角色不要主要依赖台词区分。优先让性格体现在行为选择。

当前欢迎回家高满意样本提供了一个很有效的三角色分工例子：

- 活跃角色通过蹦跳、抢先行动、滑铲、争摸等直接动作制造能量；
- 软糯 / 照顾型角色通过搬拖鞋、递物、等待表扬、蹭手等动作制造亲近感；
- 克制 / 细心角色通过收钥匙、放回物件、轻微点头、较少抢镜的动作制造温柔收束。

这说明三角色同框时，比“三个人一起冲、一起喊、一起挥手”更有效的方式是：**让每个人对同一目标做不同贡献。**

这些行为分工来自当前角色样本，不应机械复制到所有新角色；但“同一目标、不同选择”可以作为通用方法。

四小只同时出镜时：

- 菲比与咕嘎都可以活跃，但不能成为两份相同动作；菲比偏主动抢先、直接冲向目标，咕嘎偏调皮、弹簧感和半拍后的追加反应；
- 糯糯与 Sera 都可以温柔，但不能成为两份相同照顾动作；糯糯偏认真用力、软糯慢半拍和可见的重量反馈，Sera 偏安静观察、扶稳、整理、确认与克制收尾；
- 不要求平均戏份，但四人都必须对同一个事件产生可读的行动、协助、阻力、反应或收尾；
- 不安排四人整齐同步蹦跳、挥手、转头或同时发声。

若用户明确把某人指定为后景 OB，该角色不计入主线表演分工，只保持低抢戏背景行为。

## 第一帧空间优先于时间与氛围词（硬规则）

门、窗、车门、帐篷入口等把空间分成两侧的镜头，必须先锁定第一帧摄影机所在的一侧，再写时间、天气和光线。时间与氛围词不是中性的装饰；如果它们没有空间归属，模型可能先据此建立错误场景，再反向解释开门或进出动作，造成 POV 内外颠倒。

写作顺序：

1. 第一帧：摄影机位于哪一侧、朝向哪里、门／窗是否关闭、此时能看到什么。
2. 空间变化：门向哪一侧打开，哪个空间在动作后才被揭示，POV 何时跨越门槛。
3. 光线与时间：分别绑定到具体空间，例如门外为自然偏暗环境光；门内暖黄灯光只在门打开后逐渐显露并照向门外。

禁止在摄影机位置之前单独使用“傍晚时分”“夜晚暖光”“屋内灯光亮起”等没有空间归属的全局氛围词。时间并非剧情必要条件时直接删除；必须保留时，写清它属于门内还是门外。风格和氛围不得覆盖第一帧空间事实。

## POV：所有镜头移动都要有原因

真实手机 POV 不是“随机手抖”。

摄影机只因观众 / 拍摄者正在做真实动作而移动，例如：

- 拧钥匙、推门；
- 低头看脚边角色；
- 蹲下摸头；
- 伸手接盒子；
- 抬盒子到镜头前查看；
- 抬头目送角色跑远。

若发生角色扑抱、撞上、拉扯等真实接触，可以让摄影机产生对应的小幅后仰或晃动。

不要添加没有叙事动机的快速摇镜、漂移、突然跳切或“为了电影感”的随机运镜。

## 道具：单主线物件要保持实体连续性

当前披萨外送爆款样本显示，当一条视频围绕一个核心道具推进时，应明确保护这个道具的连续性。

例如“交付”类动作链：

```text
角色持有同一物件
→ 递向 POV
→ POV 双手接稳
→ 角色确认接稳后松手
→ POV 再检查 / 打开同一物件
```

需要时明确：

- 始终是同一个盒子 / 拖鞋 / 钥匙；
- 只交接一次；
- 先接稳再松手；
- 盒内内容数量保持唯一；
- 物件不要复制、变形、换款或突然重新出现在原持有者手里。

道具越承担剧情反转，连续性越重要。

## 台词、拟声词与音频

先确认角色发声权限：

- 可说完整句子；
- 只允许拟声词 / 短音节；
- 不发声，只靠动作、呼吸或环境声表演。

当前菲比、糯糯、**Q版 Sera**、咕嘎都属于“只允许专属拟声词”的类型。根据实际出镜角色分配声音；明确被指定为后景 OB 的角色默认不发声。成女／非Q版 Sera 不属于本 Skill。

多角色尤其在同一 2–3 秒阶段内，不优先安排同时发出大量声音。若需要多角色发声，优先错开；高潮结尾才考虑很短的叠加。

音频必须与动作事件和空间距离绑定。例如：

- 起跑后才发出边跑边喊的拟声词；
- 角色从近处跑向远处时，声音也从近到远；
- 睡眠治愈主题可以完全没有角色拟声词，只保留呼吸、布料、电视、门声等环境与动作声；
- 不用拟声词解释剧情，让视觉动作自己成立。

音频格式可采用：

```text
音频：[角色名] “[精确拟声词]”（真实语气 / 距离变化）
```

最终提示词只保留视频模型需要知道的实际声音，不加入符号方法论说明。

## 三种当前高价值内容结构

以下来自用户实际高满意 / 爆款样本。把它们当作可选结构库，不强制所有视频使用。

### A. 治愈观察 / 照顾型

适合“回家发现角色睡着”等低能量主题。

结构可为：

```text
进入空间并发现安静状态
→ 放轻动作靠近观察
→ 发起一个不打扰角色的照顾动作
→ 中途停顿确认没有吵醒
→ 角色出现微小无意识反馈
→ 处理环境噪音 / 光源
→ 回到角色身上安静定格
```

关键经验：

- 不需要强行反转；
- 张力来自“怕吵醒她”的动作克制；
- 微反馈比大表情重要，例如抓被角、呼吸变化、身体轻轻缩一下；
- 环境声可以承担主要音频叙事；
- 结尾通过关电视、降噪、降低动作强度完成情绪收束。

### B. 交付 → 离场 → 揭示反转型

当前披萨外送菲比爆款样本的有效结构：

```text
奇怪但一眼可懂的角色身份 / 任务出现
→ 单一主道具完成一次交接
→ 角色迅速离场
→ POV 二次检查同一主道具
→ 出现一眼可懂的小反转
→ POV 再抬头看角色远去
```

关键经验：

- 前 1–2 秒就让观众看懂“奇怪身份 / 奇怪任务”；
- 全片围绕一个主道具，避免支线抢焦；
- 角色先离场，再揭示道具里的反转，会产生更好的余韵；
- 反转用视觉证据呈现，不用台词解释；
- 角色从近到远的移动可同时带动声音距离变化；
- 结尾回到角色小小背影，形成完整闭环。

### C. 多角色欢迎回家 / 连续照顾型

历史三角色 10 秒《欢迎回家》爆款样本显示：欢迎主题可凭**连续被照顾**形成满足感。15 秒无反转版本是沿此结构提出的加强方案，不能仅凭稿件断言其成片表现。四小只版本应增加第四种行为选择，而不是复制前三人的动作。

结构可为：

```text
开门即看到角色已经在等待
→ 角色 A 做最直接的照顾动作
→ 角色 B 用更活跃 / 搞笑的方式加入
→ POV 对角色做真实回应
→ 角色 C 用克制、细心的动作补上照顾
→ 所有主线角色以不同反应接受 POV 回应
→ 形成清楚、温暖的多人结束构图
```

关键经验：

- “欢迎回家”不要只靠挥手或发声，要通过可见的照顾动作发生；
- POV 的摸头、接物等回应能显著增强“我真的回家了”的代入感；
- 多角色分别做不同事情，比同时做同一动作更有角色感；
- 结尾所有主线角色以不同姿态围住 POV 的稳定构图本身就是记忆点；
- 没有反转也可以成立，只要小奖励动作连续发生。

## 反转与社交媒体记忆点

约 15 秒且内容允许时，可使用：

```text
建立预期 A
→ 实际发生 B
→ 角色反应或视觉证据确认 B
```

反转不是强制项。

先判断内容靠什么成立：

- 靠视觉反差 / 小恶作剧 → 可以使用反转；
- 靠被照顾 / 治愈 / 微表演 → 不强行反转；
- 靠角色动作魅力 → 保持单一弧线也可以。

结尾优先留下一个容易读懂的状态：

- 明确表情 / 姿态；
- 失败后的尴尬停顿；
- 突然安静；
- 角色看向镜头；
- 小动作收尾；
- 远去的小背影；
- 多角色稳定欢迎构图；
- 可自然循环回开头的状态。

## 参考图规则

多张素材必须显式标注 `图1 / 图2 / 图3...`。

### 实际参考资产验收（有图时）

最终 Prompt 之前先看**真正要提交的视频参考文件**，不要只依据资产计划、文字描述、旧缩略图或记忆推断它是什么。逐图核对：

- 图号与角色／道具／场景职责是否对应；
- 角色身份、服装、可数配件与身体结构是否正确；
- 道具的图案、形状、持有方式等高辨识度事实是否已在图中成立；
- 场景方向和人物入场关键帧是否符合本条视频的第一可见状态。

若错误已经存在于实际参考图中，先指出具体文件与具体错误；不要试图靠视频 Prompt 与错误图片对抗。只修失败的那一项，其余已正确资产不机械重做。普通 Q版日常仍保持轻量流程：验收已有图，不等于自动新增故事板或组合资产。

每张图至少说明：

- 它是谁 / 是什么；
- 精确锁定什么；或
- 只做氛围 / 道具 / 世界观参考什么；
- 不应继承什么（有高污染风险时）。

多人角色参考尤其明确：

- 图一、图二、图三分别是不同角色时，画面有且仅有这些实体；
- 不重复生成任一角色；
- 不交换脸、发型、服装、配件、身份；
- 不继承参考图白底、原姿势和原构图；
- 参考图是 Q版 CG 时，不因为“真实手机实拍”把角色真人化。

### 四小只角色资产的参考职责

若当前输入包含四小只角色资产：

- 分别编号并说明每张图对应谁，只锁定对应角色的身份、脸、发型、服装、配件、身体比例与原有身体结构；
- 不继承参考图白底、原姿势或原构图；
- 不复制角色，不交换四人的脸、发型、服装、配件、拟声词或身份；
- 不给任何角色增加参考图中不存在的肢体；
- 用户明确要求四小只时，写清画面中有且仅有菲比、糯糯、Q版 Sera、咕嘎四个 Q版实体角色；
- 用户只点名部分角色时，实体数量跟随点名角色，不自动补齐四人；
- 场景、普通动作与普通道具仍按轻量资产默认值由提示词生成。

只有用户明确要求 Sera 作为后景 OB、彩蛋或 IP 活水印时，才读取 `references/sera-background-ob-watermark.md` 并使用该模式。提供 Sera 参考图本身不等于要求她退到后景。

换角色套用已有动作模板时，区分：

- **角色专属字段**：外观、武器、拟声词等，必须替换；
- **世界观锚定字段**：天气、场景氛围、粒子与特效语言，跟随世界观替换；
- **纯技术字段**：运镜、构图节奏、动作因果结构，可以跨角色复用。

若当前任务需要进一步判断“到底缺什么角色姿态 / 场景 / 面部资产”，只评估完成当前镜头所需的最少资产；本 Skill 不机械索要完整四视图。

## 反向约束与失败保险

不要使用固定超长 negative prompt。先找这条视频**最不能坏掉的剧情事实**，再写少量保险。

优先从以下维度推导：

### 实体数量

例如：

- 画面中只有指定角色和 POV 双手；
- 不出现额外人物或角色分身。

用户要求四小只时，应明确只有菲比、糯糯、Q版 Sera、咕嘎四个 Q版实体角色和 POV 的手，防止任一角色缺失、重复或身份互换。

用户只点名部分角色时，只保护实际出镜角色的数量。若明确使用后景 OB，分别说明“主线参与角色”与“后景 OB”，避免实体数量描述互相冲突。

### 一次性动作

若剧情只允许一次：

- 只交接一次；
- 只滑铲一次；
- 只跑远一次；
- 只把钥匙放回一次。

直接写出来，避免模型重复表演同一动作。

### 道具状态

例如：

- 始终是同一个盒子；
- 盒内始终只有一片披萨；
- 拖鞋不变成其他物品；
- 钥匙不会复制。

### 时序

例如：

- 菲比必须在起跑后才喊；
- POV 必须先接稳盒子，角色才松手；
- 打开盒子确认反转后再合上；
- 后续动作不得提前发生。

### 摄影机因果

写清 POV 只因具体动作而移动，不无故抖动、跳切或换视角。

失败保险的目标是保护**事件逻辑与连续性**，不是堆“高质量、不要畸形、不要错误”这种泛词。

## 日常生成工作流

0. 先判断是否为继承式修改；若用户要求“按原版改一下 / 保持结构只换动作”，先执行 Minimal-Delta Mode，不进入整条重写流程。
1. 先用一句话确定用户这轮要解决什么、交付到哪一步，以及暂时不做什么；只要提示词就不要自动生成视频，只要复盘就不要擅自改稿。
2. 读取并优先复用当前出镜角色已经稳定的固定层；确认角色发声权限与固定拟声词。
3. 把输入分为**已确认事实／可撤回执行假设／开放决定**。缺口只有会改变角色身份、核心事件、第一帧距离、摄影机路径、场景轴线、关键道具状态或结尾时才提问，并尽量合并成一个短问题。
4. 锁定第一可见帧：POV 在哪里、朝哪里、离主体多远；角色如何分布、已经在做什么、手中已有何物。故事从该帧直接开始，不凭空增加远距离发现或靠近过程。
5. 用一句话确定今天视频的**唯一核心事件**。
6. 选择是否属于已有高价值结构，或需要新结构。
7. 按时长分配动作 / 状态节点；所有建立、接近、缩放、寻找与转身都计入时长。
8. 检查每个关键动作是否具有“发力 → 过渡 → 结果”。
9. 多角色时先分配“谁选择做什么”，再分配声音。
10. 只补动作成立所需的场景与空间锚点；会影响画面的左右关系与路径必须写清。
11. 若有核心道具，检查它是否从头到尾是同一实体、状态是否连续。
12. 检查 POV 的每一次移动是否有真实原因，并与“路过／靠近／停留”等上层意图一致。
13. 编号所有输入参考图并标清职责；验收实际文件与图号顺序。按用户点名确定主线角色，只有用户明确要求后景 OB / IP 活水印时才启用该模式。
14. 检查同一时间片是否信息过载。
15. 做“抽象意图—具体执行一致性检查”：只读第一帧、空间句和动作句，也应得到用户原本想要的故事。
16. 从剧情最脆弱的位置推导少量失败保险。
17. 删除作者层元说明，只留下可直接交给视频模型的最终提示词。

## 默认输出结构

按任务需要输出，不强迫所有项目齐全：

```text
生成视频：[时长 / 模型 / 比例 / 成像方式]

[风格与角色固定层]

[参考图职责、实际出镜角色与实体数量；如有明确指定的后景 OB，单独标注]

场景：[只写必要空间锚点]

[若决定给模型精确时间控制]
0–Xs 画面动作：...
音频：...

[若决定让模型按自然节奏执行]
阶段／镜头1｜主要任务：...
画面动作：...
音频：...

...

【反向约束与失败保险】
[只保护本条最关键的数量、物件、时序、身份与摄影机逻辑]
```

动作时间轴的每一段优先描述“状态变化”，不要只是列动作名称。

最终提示词应像用户当前爆款样本一样：**直接、具体、可执行**。不要在成品里解释为什么这样写，也不要放给提示词作者看的方法论说明。

## 失败时怎么改

有实际成片要复查时，先保存用户已确认目标、**实际提交**的 Prompt、原参考文件及图号／上传顺序、模型设置与视频；缺项只标记未知，不用计划稿补证据。按时间点对照风格（手机实拍与 Q 版实体质感）、角色身份／数量／声音、场景道具、第一帧与 POV 路径、动作连续性，列出正确处与偏差。

对每项偏差引用对应的**原 Prompt 句子**和参考图，写清“这句话／这张图提供了什么具体画面指令 → 成片实际做了什么”。若抽象意图与具体空间动作冲突，指出冲突句；若 Prompt 未写关键状态，标记“未约束”；若输入正确却只在成片跑偏，标为模型执行偏差或待验证。单次结果只能支持合理假设，不能凭画面反推唯一致因。

先沿制作链找偏差**最早出现的阶段**：

`原始角色／场景参考 → 实际提交的视频参考资产与图号顺序 → 实际提交的 Prompt → 最终成片`

用户后来提供的原文件和实际输入优先于缩略图、计划稿与事后记忆。若错误已在参考资产里出现，先修资产；若具体 Prompt 已写出错误的空间程序，例如“远处看见 → 继续向前 → 随距离接近”，就先修 Prompt，不把结果概括成模型不懂“路过”；只有前面都正确而成片才偏离时，再判断为模型补全或执行问题。

下一轮尽量只改一个已定位变量，保留原 Prompt、实际参考文件与成片对照，不因一次结果重写全部规则。

若成片不理想，不先整份重写。优先找到唯一主故障：

- 动作跳跃 → 补中间状态；
- 结果动作不稳定 → 补动作后的衰减与最终姿态；
- 多角色混乱 → 减少同时动作 / 同时发声，重新分配行为选择；
- 角色漂移 → 恢复已验证固定层与参考图职责；
- Q版角色变真人 → 重申 Q版 CG 身份、2.5D 实体语言与禁止真人五官 / 皮肤；
- 角色像贴纸 → 补重量感、接地阴影、接触阴影和真实场景受光；
- 道具复制 / 换物 → 强化同一实体、一次性交接和状态连续；
- POV 乱晃 → 删除无动机运镜，只保留行为驱动移动；
- 节奏拖沓 → 删除无意义节点，不压缩关键过渡；
- 15 秒内容太平 → 先判断是否缺少更明确的角色行为、奖励节点或视觉反差，再决定是否需要反转；
- 信息过载 → 减少同一时间切片里的声音、动作或镜头事件；
- 四小只身份混乱或角色重复 → 恢复参考图逐一映射、实际出镜名单、专属拟声词与差异化行为；
- 明确指定的后景 OB 抢戏 → 把该角色移远、降低动作幅度、减少出现镜头，保留主线角色和 POV 为注意力中心。

新生成结果如能证明某条写法稳定有效，记录到后续实验 / 样本文件，不直接修改 Claude 原始方法论。

## 尚未验证：必须继续保留为开放问题

不得把以下问题擅自结论化：

1. 零台词写法对高能量 / 活泼角色的适用边界；
2. “减少台词”与“细化动作”各自对稳定性的独立贡献；
3. 完整反转结构在 8 秒以内的可执行下限；
4. 角色专属、世界观锚定、纯技术字段的自动分类规则；
5. 当前三个高价值内容结构在其他角色 IP 或其他视频模型上的泛化程度。

这些问题应通过后续真实生成样本继续验证。
